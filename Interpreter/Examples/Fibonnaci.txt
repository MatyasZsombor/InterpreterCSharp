let fibonacci = fn(x)
{
  let f = 0;
  let s = 1;
  
  return while (x > 0)
  {
    let x = x - 1;
    let t = f;
    
    let f = s;
    let s = t + s;
    
    t;   
  };
    
};

let y = get();
let fib = fibonacci(y);
put(fib);